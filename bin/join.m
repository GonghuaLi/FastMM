function c=join(s,varargin)
%This function is to join the string array(or cell) to a single string and
%same as perl.
%Generated by Gong-Hua Li
%2008-06-08
%modified in 2009-10-12
if nargin==1
    pat='';
elseif nargin==2
    pat=sprintf(varargin{1});
else
    error('Argument must be one or two!');
end

if iscell(s)
    for i  = 1:length(s)
       s{i}=[s{i},pat];
    end
    c = [s{:}];
    c = c(1:end-length(pat));
elseif ischar(s)
    s = [s,repmat(pat,size(s,1),1)]';
    c = s(:)';
    c = c(1:end-length(pat));
else
    error('Argument must be string array or cell!');
end

